# Terraformインフラ構成概要

このドキュメントは、Terraformインフラストラクチャの全体構成を簡潔にまとめたものです。

## ディレクトリ構造

```
infra/
├── README.md                      # このファイル
├── modules/                       # 再利用可能なTerraformモジュール
│   ├── ai_foundry/               # Azure AI Foundry
│   ├── ai_search/                # Azure AI Search
│   ├── app_service/              # App Service (Frontend/LoadBalancer)
│   ├── azure_function/           # Azure Functions
│   ├── azure_monitor/            # Azure Monitor
│   ├── azure_open_ai/            # Azure OpenAI
│   ├── common/                   # Resource Group等の共通リソース
│   ├── container_registry/       # Azure Container Registry
│   ├── cosmos_db/                # Cosmos DB
│   ├── document_intelligence/    # Document Intelligence
│   ├── event_grid/               # Event Grid
│   ├── key_vault/                # Key Vault
│   ├── log_analytics/            # Log Analytics Workspace
│   ├── microsoft_Foundry_language/ # Language Service
│   ├── private_dns_zone/         # Private DNS Zone
│   ├── private_endpoint/         # Private Endpoint
│   ├── private_link/             # Private Link
│   ├── storage_account/          # Storage Account
│   └── vnet/                     # Virtual Network
├── scripts/                       # デプロイメント用スクリプト
└── tenant/                        # テナント/環境別設定
    └── hisys/
        ├── dev/                   # 開発環境 (未実装)
        ├── staging/               # ステージング環境 (未実装)
        ├── prod/                  # 本番環境 (未実装)
        └── trial/                 # トライアル環境 (実装済み)
            ├── core/              # コアインフラストラクチャ
            │   ├── main.tf
            │   ├── provider.tf
            │   ├── outputs.tf
            │   ├── locals.tf
            │   └── environment.tf
            └── ai_service/        # AIサービス
                ├── main.tf
                ├── provider.tf
                ├── outputs.tf
                ├── locals.tf
                └── environment.tf
```

## モジュール一覧

| モジュール | 役割（一文要約） |
|-----------|----------------|
| **common** | リソースグループを作成する |
| **log_analytics** | Log Analytics Workspaceを作成し、全リソースの診断ログを集約する |
| **vnet** | VNet・サブネット・NSGを作成し、閉域ネットワークを構築する |
| **private_dns_zone** | Private DNS Zoneを作成し、Private Endpoint用の名前解決を提供する |
| **storage_account** | Storage Accountを作成し、ドキュメント・Function App用データを保管する |
| **container_registry** | Azure Container Registryを作成し、コンテナイメージを管理する |
| **cosmos_db** | Cosmos DBを作成し、メタデータ・履歴データを永続化する |
| **key_vault** | Key Vaultを作成し、シークレット・証明書を集中管理する |
| **app_service** | App Service（Frontend/LoadBalancer）を作成し、WebアプリとAPI振り分けを提供する |
| **azure_function** | Azure Functionsを作成し、サーバーレスバックエンド処理を実行する |
| **private_endpoint** | Private Endpointを作成し、Azureサービスへの閉域接続を確立する |
| **private_link** | Private Link Serviceを作成し、サービス間プライベート接続を構成する |
| **event_grid** | Event Gridを作成し、Blob追加などのイベント駆動処理を実現する |
| **azure_monitor** | Azure Monitorアラートを作成し、メトリクス監視・通知を設定する |
| **azure_open_ai** | Azure OpenAIを作成し、GPTモデル・埋め込みモデルをデプロイする |
| **ai_search** | Azure AI Searchを作成し、ベクトル・ハイブリッド検索を提供する |
| **document_intelligence** | Document Intelligenceを作成し、PDF/WordのOCR・構造化解析を実行する |
| **microsoft_Foundry_language** | Language Serviceを作成し、自然言語処理（NER等）を提供する |
| **ai_foundry** | Azure AI Foundryを作成し、AIモデル・実験を統合管理する |

---

## core / ai_service の役割

| ステート | 役割（一文要約） |
|---------|----------------|
| **core** | ネットワーク・ストレージ・認証基盤・アプリケーション層を構築する（安定基盤） |
| **ai_service** | AI関連サービス（OpenAI/Search/Document Intelligence）を構築する（変更頻度高） |

**ステート分離の理由**: AI実験・調整がコア基盤に影響しない。coreが先にデプロイされai_serviceから参照される。

---

## リソース命名規則

すべてのAzureリソースは統一された命名規則に従って命名される。

### 命名パターン一覧

| リソース種別 | 命名パターン | 例 | 備考 |
|------------|-------------|-----|------|
| **Resource Group** | `rg-{project}-{env}-{prefix}` | `rg-genashi-trial-hs` | すべてのリソースを格納 |
| **Log Analytics** | `log-{project}-{env}-{seq}` または `laws-{project}-{env}-{prefix}` | `log-genashi-trial-01` | 診断ログ集約 |
| **Storage Account** | `st{project}{env}{prefix}{suffix}` | `stgenashitrialhsmain` | 英数字のみ、最大24文字 |
| **Container Registry** | `cr{project}{env}{prefix}{seq}` または `acr{project}{env}{prefix}` | `crgenashitrial01`, `acrgenashitrialhs` | 英数字のみ、最大50文字 |
| **Cosmos DB** | `cosmos-{prefix}` | `cosmos-hs` | メタデータ・履歴保存 |
| **Key Vault** | `kv-{project}-{env}-{prefix}` | `kv-genashi-trial-hs` | 英数字とハイフンのみ、最大24文字 |
| **App Service Plan** | `asp-{prefix}-{suffix}` | `asp-hs-frontend`, `asp-hs-lb` | Frontend/LoadBalancer用 |
| **App Service** | `app-{prefix}-{suffix}` | `app-hs-frontend`, `app-hs-lb` | Webアプリ・API振り分け |
| **Function App Plan** | `asp-{prefix}-func` | `asp-hs-func` | 関数アプリ実行基盤 |
| **Function App** | `{app-name}` | `orchestrator`, `pagesplitter` | 実際のアプリケーション名 |
| **Application Insights** | `appi-{prefix}` | `appi-hs` | アプリケーション監視 |
| **Virtual Network** | `vnet-{prefix}` | `vnet-hs` | 閉域ネットワーク |
| **Network Security Group** | `nsg-{prefix}-{subnet}` | `nsg-hs-app`, `nsg-hs-func` | サブネット単位のアクセス制御 |
| **Private Endpoint** | `pep-{resource-name}` | `pep-crgenashitrial01` | Private Link接続 |
| **Diagnostic Settings** | `diag-{resource-name}` または `log-{resource-name}` | `diag-stgenashitrialhsmain-blob` | リソース診断ログ |
| **Action Group** | `ag-{project}-{env}-{prefix}` | `ag-genashi-trial-hs` | アラート通知先 |

### 命名規則の構成要素

| 要素 | 説明 | 例 |
|-----|------|-----|
| `{project}` | プロジェクト名 | `genashi` |
| `{env}` | 環境名 | `trial`, `prod`, `dev` |
| `{prefix}` | 環境識別子（長い形式） | `hs`, `genashi-trial-hs` |
| `{prefix_short}` | 環境識別子（短縮形） | `hs` (Storage/ACR等の文字数制限対応) |
| `{suffix}` | 用途・機能を表す接尾辞 | `main`, `func`, `frontend`, `lb` |
| `{seq}` | 連番 | `01`, `02`, `03` |

### 命名規則の制約

| リソース | 制約 |
|---------|------|
| **Storage Account** | 英数字のみ、3-24文字、小文字のみ |
| **Container Registry** | 英数字のみ、5-50文字 |
| **Key Vault** | 英数字とハイフン、3-24文字、先頭は英字 |
| **Cosmos DB** | 英数字とハイフン、3-44文字、小文字のみ |

### 命名規則の補足ルール

- **環境名の読み替え**: `test` は `trial` に読み替える
- **連番の扱い**: 連番（`01`, `02`, `03` や `001`, `002`, `003`）が付く場合は、プレフィクスを使わずそのまま連番を使用する
  - 例: `log-genashi-trial-01`, `crgenashitrial01`（`-hs` などのプレフィクスを含めない）

---

## Terraformファイルの役割

各ステートディレクトリ（core/ai_service）内の標準ファイル：

| ファイル | 役割（一文要約） |
|---------|----------------|
| **variables.tf** | Terraform実行時に渡す入力値（パラメータ）を定義する |
| **locals.tf** | 繰り返し使う計算値（リソース名・タグ・IPリスト等）を定義する |
| **main.tf** | 作成するAzureリソース（モジュール呼び出し）を定義する |
| **outputs.tf** | 他のステートから参照可能な値（リソースID等）を公開する |
| **provider.tf** | Azureプロバイダーとリモートステート（tfstate保存先）を設定する |
| **environment.tf** | 環境別の変数（subscription/tenant/tfstate設定）を定義する。また、外部から入力される値を定義する。 |

